{
    "name": "yulia-draw-chat",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "deno run -A build.ts",
        "watch": "nodemon --verbose app.js",
        "start": "browser-sync start --server --files '*.html, *.css, *.js'"
    },
    "dependencies": {
        "openai": "^4.73.1"
    },
    "devDependencies": {
        "browser-sync": "^2.27.7",
        "nodemon": "^2.0.22",
        "npm-run-all": "^4.1.5"
    }
}

